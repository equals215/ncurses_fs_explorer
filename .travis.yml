language: c
compiler: gcc

sudo: required
install: sudo apt-get -y install libncurses-dev

notifications:
  email:
    recipients:
    - thomas.foubert@epitech.eu
    on_success: always
    on_failure: always

script: make all clean
